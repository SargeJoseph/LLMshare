name: Build with Custom Gems
   on:
    push:
      branches: [main]  # ✅ Indentation + structure clé:valeur
   jobs:
     build:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - uses: actions/jekyll-build-pages@v1
           with:
             source: ./
             gems: jekyll-include-cache  # 👈 Force l'installation